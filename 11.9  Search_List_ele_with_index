lst = eval (input("Entre list: "))
length = len(lst)
element = int(input("Enter elements to be searched for : "))
for i in range(0,length): 
    if element == lst[i]:  
        print(element, "found at index", i)
        break
else:
    print(element, "not found in given list index")
